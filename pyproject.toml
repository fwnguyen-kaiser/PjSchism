[tool.poetry]
name = "schism"
version = "0.1.0"
description = "IOHMM crypto regime detection"

[tool.poetry.dependencies]
python = "^3.11"

[tool.pytest.ini_options]
asyncio_mode = "strict"
markers = [
    "integration: requires live DATABASE_URL (schism_db container)",
]
